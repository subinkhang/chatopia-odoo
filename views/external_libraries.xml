<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record id="menu_owl_tutorial" model="ir.ui.menu">
            <field name="name">OWL</field> <!-- Tên hiển thị -->
            <field name="sequence">1</field>   <!-- Thứ tự -->
            <!-- Không cần parent_id vì là menu gốc -->
            <!-- Không cần action -->
        </record>
        <record id="action_owl_external_libraries" model="ir.actions.client">
            <field name="name">OWL External Libraries</field>
            <field name="tag">owl.ExternalLibraries</field>
        </record>

        <menuitem id="menu_owl_external_libraries" name="External Libraries" action="action_owl_external_libraries" parent="menu_owl_tutorial" sequence="5"/>
    </data>
</odoo>